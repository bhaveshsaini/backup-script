**Script to backup any files or folders on a system (for example Raspberry pi).**

- Replace BACKUP_DEST with the save location of the backup. 
- delete_old_backup() function can be used to delete any older backups in the BACKUP_DEST directory. You'd need to uncomment it.
- Can set up a cron job to have this script run automatically after intervals.
